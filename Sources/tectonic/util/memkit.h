#pragma once

#define unsigned char tec_byte_t

#define TEC_KILOBYTES(amount) ((amount) * 1024)
#define TEC_MEGABYTES(amount) ((amount) * 1024 * 1024)
#define TEC_GIGABYTES(amount) ((amount) * 1024 * 1024 * 1024)